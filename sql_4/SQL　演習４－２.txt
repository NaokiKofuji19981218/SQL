問１新規のインデックスを作成する。

//インデックスを作成するための命令文
CREATE INDEX
  //インデックス名
  ind_rental
//対象のテーブルの指定
ON
　//テーブルの指定。
  rental
(
　//インデックス作成のための対象列
  rental_date DATE
)
;



問2新規のインデックスを作成する。

//インデックスを作成するための命令文
CREATE INDEX
  //インデックス名
  ind_order
//対象のテーブルの指定
ON
　//テーブルの指定。
  order_main
(
　//インデックス作成のための対象列
  order_date,
　//インデックス作成のための対象列
  delivery_date
)
;



問3新規のインデックスを作成する。

//インデックスを作成するための命令文
CREATE INDEX
  //インデックス名
  ind_product
//対象のテーブルの指定
ON
　//テーブルの指定。
  product
(
　//インデックス作成のための対象列
  p_name,
)
;



問4以下のコードの誤り２点を答える。

CREATE INDEX
  ind_usr
IN
  usr
(
  prefecture
  city
  o_address
) 
;


・ONが正しい。

・usrのかっこの中身は,で区切るのが正しい。



