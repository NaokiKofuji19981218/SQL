１－4

問１テーブルquestから20歳以上の人間による回答を取り出す。

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT　
　//answer1の列を取り出す。
  answer1,
　//answer2の列を取り出す。
  answer2
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  quest
//条件を決める。
WHERE
　//条件の入力
  age >= 20
;




問２テーブルbooksから、出版社が日経BP、翔泳社である情報だけを取り出す。

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT　
　//isbnの列を取り出す。
  isbn,
　// titleの列を取り出す。
  title,
　//publishの列を取り出す。
  publish
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  books
//条件を決める。
WHERE
　//条件の入力
  publish IN ('日経BP','翔泳社') 
;






問３テーブルusrから東京都に住んでない人の情報だけを出力する

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT　
　//l_nameの列を取り出す。
  l_name,
　//f_nameの列を取り出す。
  f_name,
　//emailの列を取り出す。
  email
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  usr
//条件を決める。
WHERE
　//条件の入力
  ad <> '東京都' 
;






問４テーブルquestから年齢が３０以上３９以下の型の回答だけを出力する

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT　
　//nameの列を取り出す。
  name,
　//sexの列を取り出す。
  sex,
　//prefectureの列を取り出す。
  prefecture
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  quest
//条件を決める。
WHERE
　//条件の入力
  age BETWEEN 30 AND 39 
;




問5テーブルquestから日時が２０１３年１月１日以降の回答だけを出力する

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT　
　//nameの列を取り出す。
  name,
　//answer1の列を取り出す。
  answer1,
　//answer2の列を取り出す。
  answer2
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  quest
//条件を決める。
WHERE
　//条件の入力
  date >= '2013_01_01' 
;



問6テーブルrentalから貸し出し中の情報だけを出力する

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT
　//user_idの列を取り出す。
  user_id,
　//isbnの列を取り出す。　
  isbn,
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  rental
//条件を決める。
WHERE
　//条件の入力
  id <> NULL
;


問７テーブルquestから感想欄が未定義の情報だけを出力する

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT　
　//answer2の列を取り出す。
  answer2
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  quest
//条件を決める。
WHERE
　//条件の入力
  comment <> NULL
;



問8以下のコードから間違いを2点答える。

SELECT
  title,
  publish,
  price
FROM
  books
WHERE 
  price <= 500
; 

・titleとpublishの横にそれぞれ , を付ける。

・priceの横は < が正しい。

























