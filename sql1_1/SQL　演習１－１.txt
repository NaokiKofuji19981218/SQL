１ー１


問１テーブルbooksからすべての列を取り出す。

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT
  //すべての列を取り出す。
  *
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  books
;




問２テーブルauthorからすべての列を取り出す。

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT
  //すべての列を取り出す。
  *
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  author
;



問３テーブルsalesからすべての列を取り出す。

//SELECT命令を出力する。テーブルからデータを取り出す。
SELECT
  //すべての列を取り出す。
  *
//何のテーブルからデータを取り出すか
FROM
  //参照するテーブル名の入力
  SALES
;



問４以下のコードから間違いを2点答える。

select
  +
from
  books
;


・大文字にしなくてはいけない。

・すべての列を表示するには、*　を使う。